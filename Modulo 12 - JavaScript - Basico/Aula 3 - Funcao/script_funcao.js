/* //Funcao;

function media(n1, n2) {
  var media = (n1 + n2) / 2;
  console.log(media);
}

media(10, 5); 

function media(n1, n2) {
  var media = (n1 + n2) / 2;
  return media;
}

var aluno = media(2, 4);
console.log(aluno);
 */

/* //Funcao anononima
var media = function (n1, n2) {
  return (n1 + n2) / 2;
};

console.log(media(10, 8));
 */

/* // Arrow funcion
var media = (n1, n2) => {
  return (n1 + n2) / 2;
};

console.log(media(10, 8));
 */